import { PatternPaneConfig } from "./config.js";

class PatternPane {
    constructor(config) {
        this.config = config;
    }
}
PatternPane.Config = PatternPaneConfig

export {
    PatternPane
}